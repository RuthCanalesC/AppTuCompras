# ============================================================
# TUCOMPRAS API - Variables de entorno (plantilla)
# Copie este archivo como ".env" y complete los valores reales.
# El archivo .env NUNCA se sube a Git (está en .gitignore).
# ============================================================

# Servidor
PORT=4000
NODE_ENV=development

# Base de datos MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=admin_tucompras
DB_PASSWORD='Adm1n#TuC0mpras2026!'
DB_NAME=tucompras_db
DB_CONNECTION_LIMIT=10
